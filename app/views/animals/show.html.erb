<h1><%= @animal.name %></h1>

<p>
  <% if @animal.photo.attached? %>
    <strong>Photo:</strong>
    <%= image_tag url_for(@animal.photo) %>
  <% end %>
</p>

<p>
  <strong>Gender:</strong>
  <%= @animal.gender %>
</p>

<%= link_to 'Edit', edit_animal_path(@animal) %> |
<%= link_to 'Back', dashboard_path %>